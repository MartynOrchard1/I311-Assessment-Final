<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Product Catalog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

  <header class="p-3 bg-dark text-white">
    <div class="container">
      <h1><a href="/" class="text-white text-decoration-none">Product Catalog</a></h1>
      <nav>
        @if(auth.user)
          <form action="{{ route('logout') }}" method="POST" class="d-inline">
            @csrf()
            <button class="btn btn-danger btn-sm">Logout</button>
          </form>
        @else
          <a href="{{ route('login.show') }}" class="text-white">Login</a>
        @endif
      </nav>
    </div>
  </header>

  <main class="container mt-4">
    {{{ await $slots.main() }}}
  </main>

  <footer class="text-center mt-4">
    &copy; {{ new Date().getFullYear() }} Product Catalog — Martyn Orchard - 21356032
  </footer>

</body>
</html>
